Flask==3.0.0
gunicorn==21.2.0
requests==2.32.3
python-dotenv==1.0.1
# urllib3 se usa directamente en listener.py (Retry, HTTPAdapter), no solo como
# dependencia transitiva de requests, asi que va declarada.
urllib3==2.2.3
